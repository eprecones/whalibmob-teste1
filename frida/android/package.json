{
  "name": "whalibmob-frida-android",
  "version": "1.0.0",
  "description": "Android Play Integrity / Keystore attestation server for whalibmob registration (derived from Auties00/cobalt)",
  "private": true,
  "main": "demo.js",
  "scripts": {
    "prepare": "npm run build",
    "build": "frida-compile server.js -o server_with_dependencies.js",
    "watch": "frida-compile server.js -o server_with_dependencies.js -w"
  },
  "devDependencies": {
    "@types/frida-gum": "^18.5.1",
    "@types/node": "^18.19.3",
    "frida-compile": "16.4.1"
  }
}
