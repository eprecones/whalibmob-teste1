# whalibmob — example environment configuration
#
# Copy this file to `.env` in your project root and uncomment what you need:
#
#     cp node_modules/whalibmob/.env.example .env
#
# The CLI loads `.env` from the current working directory automatically — from
# the directory the command runs in, not from where whalibmob is installed. Run
# `wa` somewhere else and this file does not apply; export the variables in your
# shell profile instead if you want them everywhere.
# If you use the library programmatically, call `require('dotenv').config()`
# yourself before creating a client.
#
# Every variable below is OPTIONAL — with an empty .env the library runs with
# the defaults shown next to each entry.


# ─── Device profile ──────────────────────────────────────────────────────────
# Which platform to identify as during registration and connection.
# Values: ios | android
# CLI equivalent: --platform iphone|ios|android (or --iphone/--ios/--android).
# The flag only selects new sessions; an existing session keeps its stored device.
# WA_OS=ios

# Register as WhatsApp Business instead of the consumer app.
# Values: 1 | true | yes | on  (anything else, or unset, means the consumer app)
#
# This has to be set before the code is requested: an account is created as
# Business or as consumer, and the token, the User-Agent and the verified-name
# certificate all have to keep saying which. A session already part-way through
# registering keeps what it started as and says so.
#
# On Android the token material is read from com.whatsapp.w4b rather than
# com.whatsapp, and is cached in its own file, so the two never overwrite each
# other. On iOS the version comes from the Business App Store listing.
# WA_BUSINESS=1

# Named predefined device profile. Takes priority over the individual
# WA_DEVICE_* variables below.
#
#   ios      iphone15pro, iphone15, iphone14, iphone14pro, iphone13, iphone12
#   android  samsung-s24-ultra, samsung-s24, samsung-s23, samsung-s23-ultra,
#            samsung-a55, pixel8pro, pixel8, pixel7, pixel7a, xiaomi14,
#            xiaomi13, oneplus12, oneplus11, oppo-find-x7, realme-gt5
#
# Default: iphone15pro when WA_OS=ios, samsungs24ultra when WA_OS=android
# WA_DEVICE=iphone15pro


# ─── Custom device profile ───────────────────────────────────────────────────
# Only used when WA_DEVICE does not match a predefined profile. Any value left
# unset falls back to the default profile for the selected WA_OS.
# The defaults shown here are the iOS ones (iPhone 15 Pro).

# Device display name — e.g. "iPhone 15 Pro", "Samsung Galaxy S24 Ultra"
# WA_DEVICE_MODEL=iPhone 15 Pro

# Apple | Samsung | Google | Xiaomi | OnePlus …
# WA_DEVICE_MANUFACTURER=Apple

# OS version string — e.g. 17.4.1 for iOS, 14 for Android
# WA_DEVICE_OS_VERSION=17.4.1

# OS build number — e.g. 21E236 for iOS, UP1A.231005.007 for Android
# WA_DEVICE_BUILD=21E236

# Internal model identifier — e.g. iPhone16,1 for iOS, SM-S928B for Android
# WA_DEVICE_MODEL_ID=iPhone16,1


# ─── WhatsApp version ────────────────────────────────────────────────────────
# LEAVE THIS UNSET. It is a temporary override, not a setting.
#
# When set, the live version lookup is skipped and this value is announced on
# connect in place of the version the session was registered with — every
# connect started from this directory, for every number, until it is removed.
# The version WhatsApp accepts moves on every few weeks, so a value left here
# eventually refuses every connect with `<failure reason="405">`, on sessions
# that were working the day before. It is the single most common cause of that
# error.
#
# On Android it is never needed: the version comes out of the APK the
# registration token was signed over, which is the build the token actually
# proves. Pinning something else here makes the two describe different builds.
#
# On iOS it is the escape hatch: the version is looked up on the App Store, and
# a lookup that times out leaves a built-in fallback in the session that the
# server may refuse. Pin a current version, connect, then remove the line —
# uncomment this only when you have a 405 and a version to put in it.
# WA_VERSION=

# Override the static registration token. iOS only — Android has no static token
# and signs its own out of the APK (see below). Leave unset to use the built-in
# one; only change this if you know what you are doing.
# WA_STATIC_TOKEN=


# ─── Android registration token material ─────────────────────────────────────
# Registering with WA_OS=android needs material read out of a WhatsApp APK. The
# first registration fetches the APK from Google Play and does this on its own —
# nothing to run first. To do it by hand, from a Play download or an APK you
# already have:
#
#     wa apk-material --download
#     wa apk-material base.apk [split_config.xxhdpi.apk ...]
#
# It is written to <session dir>/android-apk-material.json by default. Set this
# to keep it somewhere else. Registering as iOS needs none of it.
# WA_ANDROID_APK_MATERIAL=/path/to/android-apk-material.json

# Never fetch the APK automatically. Registration then fails with instructions
# instead of pulling a hundred megabytes unasked.
# WA_NO_APK_DOWNLOAD=1

# Register with material from an APK that is not signed by WhatsApp. Refused by
# default, because a re-signed APK yields a token that belongs to nobody and
# spends code requests against your number for nothing.
# WA_ALLOW_FOREIGN_APK=1


# ─── Flash call verification ─────────────────────────────────────────────────
# --method flash has WhatsApp ring the number and hang up; the code is the last
# digits of the number that called. Six is what the app defaults to; set this
# only for a number whose server expects a different length.
# WA_FLASH_CODE_LEN=6


# ─── Proxy ───────────────────────────────────────────────────────────────────
# Route every outbound connection through a SOCKS proxy: the message socket
# (mobile TCP and web WSS alike), registration, the announced-version lookups,
# the APK material download and media uploads. Requires the `socks` package:
# npm install socks
#
# A bare host:port is assumed to be socks5. Add a scheme to be explicit, and
# credentials if the proxy needs them. Percent-encode any @ or : in a password.
#
#   TOR_PROXY takes precedence over SOCKS_PROXY when both are set.
#   The default Tor SOCKS port is 9050; the SOCKS default is 1080.
#
# TOR_PROXY=127.0.0.1:9050
# TOR_PROXY=socks5://127.0.0.1:9050
# SOCKS_PROXY=socks5://user:pass@proxy.example.com:1080
# SOCKS_PROXY=socks5h://proxy.example.com:1080
# SOCKS_PROXY=socks4://proxy.example.com:1080
#
# Only if `socks` lives somewhere require() cannot find it:
# WA_SOCKS_LIB=/absolute/path/to/socks/build/index.js

# ─── Push token ──────────────────────────────────────────────────────────────
# Every WhatsApp on a real phone holds a Firebase push token — it is how the
# server wakes the app. Registration fetches one from Google and ships it as
# push_token, so the request looks like the install it claims to be.
#
# On by default. Three plain HTTPS calls to Google, once per number, cached with
# the session afterwards. Any failure is silent: the field is dropped and
# registration proceeds exactly as it would without it.
#
# Set to 0 to skip it entirely.
# WA_FCM_PUSH=0


# ─── Registration funnel telemetry ───────────────────────────────────────────
# Registration funnel telemetry is a separate external side effect. It is off by
# default; set to 1 only when you explicitly want /client_log events in addition
# to the requested registration endpoint. Failures are swallowed.
# WA_FUNNEL_LOG=1


# ─── Frida attestation bridge ────────────────────────────────────────────────
# Host running the Frida attestation helper (see the frida/ directory).
# Leave unset to disable the bridge.
# WA_FRIDA_HOST=127.0.0.1

# Port the helper listens on.
# Default: 1119 (WhatsApp consumer; the Business build listens on 1120)
# WA_FRIDA_PORT=1119

# Fail an Android registration unless the optional device bridge /info endpoint
# confirms that the installed app version equals the APK token material.
# This does not mint or bypass attestation; it is only a local consistency guard.
# WA_REQUIRE_INSTALLED_APK_MATCH=1
